name: worker

on:
  push:

jobs:
  clue:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        experience: ["angry", "aluminium", "resume", "plane", "stair"]
        revenge: ["lover", "exceedingly", "packet", "suspend"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar hR
